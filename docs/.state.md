# Memory State

```mermaid
flowchart LR
    Design["Product + architecture approved"] --> Slice["Phase 0 vertical slice verified"]
    Slice --> Pointer["S1 Pointer verified"]
    Pointer --> Stroke["S2 freehand transport verified"]
    Stroke --> Sketch["S3 deterministic Sketch verified"]
    Sketch --> Text["S4 text and IME protocol verified"]
    Text --> Patch["S5 ScenePatch verified"]
    Patch --> Scale["S6 SVG budget verified"]
    Scale --> Persistence["S7 atomic persistence verified"]
    Persistence --> Migration["S8 migration verified"]
    Migration --> Lease["S9 single-writer verified"]
    Lease --> Operation["S10 operations verified"]
    Operation --> Lifecycle["S11 host lifecycle verified"]
    Lifecycle --> Tooling["S12 tooling verified"]
    Tooling --> Complete["Phase 0 S0-S12 complete"]
    Complete --> CI["PR and main CI"]
    CI --> Phase1A["Phase 1A local persistence integrated"]
    Phase1A --> Camera["Camera + viewport + fit content integrated"]
    Camera --> Selection["Rust-owned selection + delete integrated"]
    Selection --> Freehand["Rust-owned Select/Draw + product freehand"]
    Freehand --> ProductText["Fixed-font multiline text + IME"]
    ProductText --> StyleProfile["Persistent styles + profile compatibility"]
    StyleProfile --> CleanSurface["Clean-only product surface"]
    CleanSurface --> Phase1B["Phase 1B editor foundation"]
    Phase1B --> Stability["Gesture + Shift + text stability"]
    Stability --> StrokeProjection["Transform-independent stroke projection"]
    StrokeProjection --> InterruptionCommit["DOM interruption-safe transform commit"]
    InterruptionCommit --> SmoothStroke["Deterministic smooth freehand geometry"]
    SmoothStroke --> LeaseTakeover["Explicit verified lease takeover"]
    LeaseTakeover --> BasicShapes["Schema V4 basic shapes"]
    BasicShapes --> ShapeCreation["Rust-owned direct shape creation"]
    ShapeCreation --> VertexEditing["Rust-owned path vertex editing"]
    VertexEditing --> ElementSize["Schema V5 element-level Size"]
```

- Last reviewed commit: `4cfc33a` plus the current `codex/shape-vertex-editing` worktree
- Iteration: `35`
- Last run: `Doubled Arrow head metrics to match the user-provided open-arrow visual reference`
- Covered areas: product/architecture decisions, Rust-WASM-Web ownership, package structure, Vite+ and official-registry workflow, GitHub Actions gate, >=90% coverage policy, interaction/rendering spikes, integrated persistence/migration/single-writer startup, Camera/Viewport session state, Rust Editor State selection, Diagram Operation V1, framework-neutral lifecycle, React/Vue/Vanilla hosts, persistent element styles, transform-independent stroke projection, transform-stable Arrow geometry, DOM interruption-safe transform commit, deterministic smooth freehand geometry, internal deterministic Sketch compatibility, the Clean product baseline, the Phase 1B editor foundation, Schema V4 basic shapes, Rust-owned direct shape creation, cross-tool pointer/capture stability, Rust-owned Line/Polyline/Arrow vertex editing, and Schema V5 element-level S/M/L/XL Size
- Verification evidence: the full gate passes `pnpm install --frozen-lockfile`, `pnpm check`, 436 Web tests, 145 Rust tests, Web/Rust coverage, the real WASM rebuild, and the production build. Web coverage is 95.31% statements, 90.96% branches, 95.75% functions, and 95.56% lines; Rust coverage is 92.30% regions, 92.20% functions, and 93.33% lines. Real-WASM Vanilla inspection at `r530 / 7 elements / 87%` verified that the M Arrow resolves a 40-unit head length and 36-unit opening instead of 20/18, while the shaft width, persistent tip, angle, direction, vector sharpness, bounds, and hit geometry remain stable. The browser console stayed clean, and the combined reference/before/after comparison passed with no actionable P0, P1, or P2 visual differences.
- Open risks: P-05 recovery-copy UX, fixed-font bundle size calibration, semantic Text resize specialization, content spans that still exceed the viewport at the absolute 10% Camera floor, low-end SVG calibration, real physical pen/coalescing device behavior, deeper multi-selection accessibility polish, future midpoint curve editing for Line/Arrow, future Connector binding/routing semantics, element-level dash/hand-drawn styles, legacy global `renderProfile` migration/removal, and pressure-aware variable-width freehand

---
*Last updated: 2026-07-23 | Reason: record the reference-matched larger Arrow head and real-WASM visual QA*
